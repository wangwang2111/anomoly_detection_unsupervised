name: anomaly-detection
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas
  - numpy
  - scikit-learn
  - matplotlib
  - seaborn
  - openpyxl
  - jupyter
  - ipykernel
  - pip
  - pip:
      - mlflow
      - pyod
      - combo
      - pyyaml
      - plotly
      - pytest
      - pytest-cov
      - nbformat
